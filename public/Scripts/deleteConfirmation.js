function confirmDelete() {
    // This function displays a confirmation dialog box when called
    return confirm("Are you sure you want to delete this assignment?");
    // The confirm() method displays a modal dialog with a specified message and two buttons, 'Ok' and 'Cancel'.
    // It returns true if the user clicks 'Ok' and false if the user clicks 'Cancel'.
}
